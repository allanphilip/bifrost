{
	"type": "module",
	"name": "bifrost_poc",
	"version": "1.0.0",
	"description": "Mocha Super Test Chai",
	"main": "index.js",
	"scripts": {
		"test": "mocha tests --timeout 5000",
		"report": "mocha tests --reporter mochawesome --timeout 5000",
		"junit" : "mocha tests --reporter mocha-junit-reporter"
	},
	"keywords": [
		"supertest"
	],
	"author": "TestHouse",
	"license": "BiFrost",
	"devDependencies": {
		"chai": "^4.3.6",
		"mocha": "^10.0.0",
		"mocha-junit-reporter": "^2.0.2",
		"mochawesome": "^7.1.3",
		"supertest": "^6.2.3"
	},
	"dependencies": {
		"xhr2": "^0.2.1"
	}
}
